import AdminAppointments from "@/components/dashboardPage/admin/AdminAppointments";

export const metadata = {
  title: "All Appointments",
};

export default function AdminAppointmentsPage() {
  return <AdminAppointments />;
}
